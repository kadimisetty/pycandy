"[Working with Unicode](http://vim.wikia.com/wiki/Working_with_Unicode)

"Use in you vim configuration file

if has("multi_byte")
    if &termencoding == ""
        let &termencoding = &encoding
    endif
    set encoding=utf-8
    setglobal fileencoding=utf-8
    "setglobal bomb
    set fileencodings=ucs-bom,utf-8,latin1
endif

